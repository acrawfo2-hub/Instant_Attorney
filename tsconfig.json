{
  "extends": "./tsconfig.base.json",
  "compileOnSave": false,
  "files": [],
  // No project references: the shared lib/* packages (db, api-spec, api-zod,
  // api-client-react) existed only for the deleted Express api-server. The
  // product in artifacts/instant-attorney has its own tsconfig and is
  // deliberately outside this workspace.
  "references": []
}
